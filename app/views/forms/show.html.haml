.container.bp
  .col-md-9.col-md-offsenotebook
    - if @form.state == "ALL"
      %h1= @form.name
    - else
      %h1= (@state_full + " " + @form.name)
    %h2= @form.department
    %p= @description
  .col-md-10.col-md-offset-1
    .btn1
      = link_to "View Document", asset_path(@form.location), target: :_blank